version: "2"
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - revive
  settings:
    revive:
      rules:
        - name: exported
          arguments: [checkPrivateReceivers]

